spring:
  main.banner-mode: 'off'
  profiles.active:
    - database
logging:
  file.name: target/test.log
  level: # TRACE < DEBUG < INFO < WARN < ERROR < FATAL < OFF
    root: ERROR
    tech.simter.r2dbc: WARN
    #org.springframework: WARN
    #reactor: INFO
    #reactor.Mono: INFO